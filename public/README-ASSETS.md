# Placeholder assets

Replace these before deploying:

- `favicon.svg` — swap for your own mark, or keep the default terminal glyph.
- `og-image.png` (1200x630) — add a real Open Graph preview image referenced in `index.html`.
- `resume.pdf` — add your resume here and update `VITE` config / `src/data/profile.ts` `resumeUrl`.
